# docker-compose 用の環境変数テンプレート。
# このファイルをコピーして .env を作成し、値を設定してください。
#   cp .env.example .env
# .env は .gitignore で除外されるため、認証情報は公開されません。

MYSQL_DATABASE=oursle_wp
MYSQL_USER=oursle_wp
MYSQL_PASSWORD=change_me
MYSQL_ROOT_PASSWORD=change_me_root
